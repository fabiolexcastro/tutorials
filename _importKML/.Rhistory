files <- list.files('/_data')
files
files <- list.files('_data')
files
files <- list.files('_data', full.names = TRUE, pattern = '.kmz$')
files
require(raster)
require(rgdal)
require(plotKML)
require(maptools)
require(XML)
getKMLcoordinates(files[[1]], ignoreAltitude = T)
readLines()
